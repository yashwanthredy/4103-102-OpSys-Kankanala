#!/bin/bash
echo command_args.sh             #print the name of the file
echo \(Contents of arg1\)
b=0         #print the line to the prompt
for i in $a                            # creating a loop for all the args to print in the next line
do                            #printing the args from the command line                               # end of for loop
b=$(( $i + $b ))
#printf $i+
done
echo $b
#echo $a , $b
#echo c
