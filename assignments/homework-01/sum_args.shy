#!/bin/bash
echo command_args.sh             #print the name of the file
echo \(Contents of arg1\)         #print the line to the prompt
for i;                            # creating a loop for all the args to print in the next line
do
echo $i                            #printing the args from the command line
done                               # end of for loop
c=$(( a + b ))
echo $c
#echo $a , $b
#echo c
